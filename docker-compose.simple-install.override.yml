version: '3.9'
services:
  mysql:
    networks:
      - montra_internal_network
  redis:
    networks:
      - montra_internal_network
  eramba:
    networks:
      - montra_internal_network
  cron:
    networks:
      - montra_internal_network
networks:
  montra_internal_network:
    external: true